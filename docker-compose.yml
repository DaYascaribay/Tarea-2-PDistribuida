version: '3.8'

services:
  db1:
    image: mysql:8.0
    restart: always
    environment:
      MYSQL_DATABASE: dbUsers
      MYSQL_ROOT_PASSWORD: example1
    ports:
      - "3306:3306"
    volumes:
      - ./DB/init.sql:/docker-entrypoint-initdb/init.sql

volumes:
  mysql_data: